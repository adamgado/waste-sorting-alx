import { HighlightcardDirective } from './highlightcard-directive.directive';

describe('HighlightcardDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightcardDirective();
    expect(directive).toBeTruthy();
  });
});
